<module name="Synth" var-name="Synth" interface="normal">

  <!-- ================================================ -->

  <param name="Progress"
         default="0"
         transform="Range[0.f, 100.f]"
         format="Percent"
         interface="$self.progress = $value"/>

  <!-- ================================================ -->

</module>