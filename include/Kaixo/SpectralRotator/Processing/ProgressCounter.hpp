#pragma once

// ------------------------------------------------

#include "Kaixo/Core/Definitions.hpp"

// ------------------------------------------------

namespace Kaixo::Processing {

    // ------------------------------------------------

    class ProgressCounter {
    public:

        // ------------------------------------------------

        class ScopedFinish {
        public:

            // ------------------------------------------------

            ~ScopedFinish();
            ScopedFinish(ProgressCounter* self);

            // ------------------------------------------------

        private:
            ProgressCounter* m_Self;

            // ------------------------------------------------

        };

        // ------------------------------------------------

        ScopedFinish scoped();

        // ------------------------------------------------

        void reset();
        void increaseEstimate(std::int64_t v);
        void step();
        void done();

        // ------------------------------------------------

        float progress() const;

        // ------------------------------------------------

    private:
        std::atomic_int64_t m_Estimate = 1;
        std::atomic_int64_t m_Steps = 0;

        // ------------------------------------------------

    };

    // ------------------------------------------------

}

// ------------------------------------------------
